{
  "name": "agent-code-reviewer",
  "version": "1.0.0",
  "description": "GitHub Action that reviews pull requests with Claude (security, bugs, performance, style)",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "all": "npm run typecheck && npm run build"
  },
  "keywords": [
    "github-action",
    "code-review",
    "claude",
    "anthropic",
    "ai"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@anthropic-ai/sdk": "^0.102.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0"
  }
}
